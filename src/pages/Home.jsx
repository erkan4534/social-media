import PropTypes from "prop-types";

const Home = (props) => {
  return (
    <div>
      <h1 className="text-3xl font-bold underline">Hello world!</h1>
    </div>
  );
};

Home.propTypes = {};

export default Home;
