import PropTypes from "prop-types";
import "./Home.css";
import Header from "../../layouts/Header";

const Home = (props) => {
  return (
    <div>
      <Header></Header>
    </div>
  );
};

Home.propTypes = {};

export default Home;
